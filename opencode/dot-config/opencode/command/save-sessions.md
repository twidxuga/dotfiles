---
description: Append currently running opencode sessions (id, title, folder, pid) to ~/.opencode-sessions
---

Run `~/.config/opencode/scripts/save-sessions.sh` via bash to append the list of currently running opencode sessions to ~/.opencode-sessions. After it completes, show the user the last 30 lines of ~/.opencode-sessions so they can see what was just appended.
